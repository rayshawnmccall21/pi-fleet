{
  "name": "pi-fleet",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-Device Agent Orchestration Platform",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "ai-agents",
    "orchestration",
    "multi-device",
    "pi-agent"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
